require 'spec_helper'

describe ChwinksController do
  context "Sharing links" do
    it "should redirect to facebook popup when clicked on link"
    it "should redirect to twitter popup when linked on link"
    it "should redirect to chwink's id page when clicked on I would like to vote"
  end
end
